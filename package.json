{
  "name": "json-file-server",
  "version": "0.2.1",
  "description": "An API to serve JSON files from disk.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node index.js"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@fastify/cors": "^8.3.0",
    "@fastify/static": "^6.10.2",
    "fastify": "^4.18.0"
  }
}
